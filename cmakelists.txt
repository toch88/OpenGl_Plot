cmake_minimum_required(VERSION 3.0.0)
project(Cherno_OpenGL VERSION 0.0.0)

include(CTest)
enable_testing()

include_directories(C:/msys64/mingw64/include)

find_library(OPENGL libopengl32.a C:/msys64/usr/lib/w32api)
find_library(GLFW3 libglfw3.a C:/msys64/mingw64/lib)

add_executable(Cherno_OpenGL main.cpp)
message("======${OPENGL} =========== \n")
message("------${GLFW3} ------------ \n")
target_link_libraries(Cherno_OpenGL ${OPENGL} ${GLFW3})

set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
